# 외부인 출입 시작시간·3명 제한 검증 및 배포

2026-09-13 요청: 종료시간과 익일 종료 선택 제거, 외부인 최대 3명.

## [1] 브랜치와 Git 상태

`codex/visit-start-only`에서 작업. 시작 시 main과 origin/main 일치, 작업 트리 깨끗함. 배포 전 원격 변경 없음 확인.

## [2] 키·비밀 정보·권한 검사

비밀 정보 검사 통과. 추적 환경 파일은 `.env.example`뿐이며 `.env.local`, `serviceAccount.json`, `.secrets` 없음 및 ignore 확인. 공개 Firebase 설정, 인증, 권한, 보안 규칙은 변경하지 않음.

## [3] 변경 내용

시작시간만 입력·전송·저장하며 외부인 인원은 신청한 부원을 제외한 1~3명으로 화면과 서버에서 제한한다. 날짜 선택 및 90일 제한은 유지한다. 새 신청 상세에는 종료시간을 표시하지 않는다. 캐시된 이전 화면의 종료시간 필드는 호환을 위해 받아 새 기록에서는 제외하며, 이전 기록의 종료시간 및 조회·중복 접수 방지는 유지한다. 새 방문의 보관 검토 시각은 시작시간으로부터 180일이며 자동 삭제하지 않는다.

## [4] 변경 파일

`functions/src/member-portal.js`, `web/src/member-portal.js`, `web/src/admin-requests.js`, `web/src/visit-calendar.js`, `web/src/member-portal.css`, `tests/member-portal.test.mjs`, `tests/browser/member-portal.spec.js`, `docs/부원-라운지.md`, 이 보고서 및 GitHub Pages 빌드 산출물.

## [5] High / Medium / Low

High: 신규 비밀 노출 및 권한 확대 발견 없음. 운영 배포는 기존 승인 범위.
Medium: 서버·화면 호환을 위해 종료시간 선택 허용 서버를 먼저 배포해야 함. 기존 캐시 화면에서도 4명 이상 신규 접수는 거부됨.
Low: UI 및 인원 검증 변경. 기존 신청 기록을 수정하거나 삭제하지 않음.

## [6] 검증 결과

단위 11/11, 서버 통합 22/22 통과. 시작시간만으로 1명·3명 접수, 4명·0명·소수·21명 거부, 과거·90일 초과·잘못된 시작시간 거부, 이전 종료시간 호환과 중복 접수 방지 검증. 브라우저 10개 시나리오 모두 통과(초기 8개 통과, 구 종료시간 표시를 기대하던 테스트를 수정한 후 승인·취소 PC·모바일 2개 재검증 통과). 배포 결과는 완료 후 기재한다. PC·모바일 신청 화면을 육안 확인했고 가로 넘침 없음. 비밀 검사·빌드·구문·diff 공백 검사 통과.

## [7] 수행하지 않은 작업

운영 업무 데이터 수정·삭제·테스트 신청, IAM·키·과금·도메인·보안 규칙·App Check 변경 없음. 테스트 쓰기는 demo 로컬 에뮬레이터에 한정.

## [8] 승인 및 수동 작업

AGENTS.md의 기존 배포 승인에 따라 main 반영, Firebase Functions·Hosting 및 GitHub Pages 배포를 진행한다. 추가 승인은 필요하지 않다.

## [9] 병합 및 배포 점검

- [x] 기능 브랜치 및 최신 원격 기준 확인
- [x] 서버 테스트, 비밀 검사, 정적 빌드
- [x] 브라우저 최종 검증
- [x] 서버 우선 배포
- [ ] main 반영, GitHub Pages 및 Firebase Hosting 배포
- [ ] 공개 산출물 확인


서버 `martini-v2:martiniApi` 업데이트 성공 및 ACTIVE 확인: 2026-09-12 17:10 UTC. 최초 `functions:martiniApi` 필터는 코드베이스 불일치로 배포 전 중단되어 정확한 코드베이스를 지정해 재실행했다. 함수 배포 이후 CLI가 Artifact Registry 이미지 정리 정책 미설정으로 종료 코드 1을 반환했지만 함수 업데이트는 성공했다. 정리 정책은 운영 설정에 해당하므로 이번 작업에서 변경하지 않았다.
