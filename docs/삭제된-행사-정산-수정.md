# 삭제된 행사 정산 정정 · 2026-09-09

[1] 브랜치: `codex/archived-event-settlement`에서 구현하고 검증 후 main에 반영한다.

[2] 비밀·권한 점검: `check:secrets` 통과. 민감 파일 패턴 중 추적 파일은 `.env.example`만 해당한다. 기존 재정 권한 검사를 유지하며 IAM·인증·Security Rules·키 설정을 변경하지 않는다.

[3] 변경: 원본이 보관된 행사·신청의 삭제 표시는 연결된 장부 정정을 차단하지 않는다. X로 잘못된 거래 기록을 삭제하면 기존과 같이 납부·환불 합계를 트랜잭션으로 정정한다. 행사·신청의 삭제 상태 및 공개 링크 차단을 유지한다. 실제 원본이 없거나 재신청으로 연결 대상이 바뀌면 계속 차단한다. 환불액이 납부액을 초과하는 정정도 차단한다.

[4] 파일: `functions/src/deletion.js`, `tests/integration.test.mjs`, 이 문서. 클라이언트 변경은 없다.

[5] 위험: High — 재정 담당자의 실행 시 장부와 연결 합계를 변경할 수 있으므로 기존 권한·확인·동시성·중복 방지 검사를 유지한다. Medium — 삭제 기록의 정정과 복구를 혼동할 수 있으므로 복구하지 않음을 명시한다. Low — 화면 변경 없음.

[6] 검증: 삭제된 완료 행사에서 납부 정정, 삭제된 행사·신청에서 환불 후 납부 정정, 삭제 상태 보존, 공개 링크 차단, 중복 요청, 무권한 요청, 원본 누락·재신청 차단을 통합 테스트에 추가했다. 최종 결과는 배포 기록에 기록한다.

[7] 실제 운영 자료는 조회·수정·삭제하지 않았다. 테스트 자료 변경은 로컬 demo 에뮬레이터에서만 실행했다.

[8] main 반영과 Firebase 배포는 기존 프로젝트 승인을 적용한다. 실제 정산 정정은 운영진이 기존 X 확인 화면에서 실행한다.

[9] 체크리스트: 비밀 검사, 통합 테스트, 함수 배포 성공 확인, main push 및 Pages 빌드 확인. 클라이언트 파일은 기존 배포본을 유지한다.

## 배포 기록

검증 및 배포 완료 후 결과를 기록한다.
