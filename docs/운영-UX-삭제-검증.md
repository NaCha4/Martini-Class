# 운영 화면 탐색 및 삭제 기능 개선 · 2026-09-09

## [1] 브랜치와 Git 상태

기능 브랜치: `codex/operations-ux-delete`. 기존 회의록 작업은 보존했으며 해당 작업이 main에 반영된 뒤 그 위에서 진행했다. 구현 커밋 `3472646`을 main에 fast-forward 반영하고 origin/main에 push했다. 최종 작업 브랜치는 main이다.

## [2] 비밀 정보와 권한 점검

`npm run check:secrets` 통과. 검사 대상 소스에서 개인 키·서비스 계정 서명을 발견하지 않았고 `.env.local`, `serviceAccount.json`, `.secrets`는 없었다. 민감 파일 패턴의 Git 추적 결과는 안전한 예제인 `.env.example`만 해당한다. 공개 Firebase 설정은 기존 값을 유지한다. IAM, 로그인, API 키, Security Rules, 과금 설정은 변경하지 않았다.

삭제 요청은 서버가 기존 업무 권한을 확인하고 허용된 자료 종류만 받는다. 확인 값, 수정 시각과 버전을 검증한다. 확정 회의록은 기존 정정 권한과 동일하게 회장단 권한이 필요하다.

## [3] 변경 내용

- 운영 메뉴를 활동 운영 / 운영 지원 / 관리·설정으로 분류하고 업무 키워드로 메뉴를 검색한다.
- 운영 홈에 주요 업무 바로가기를 제공한다.
- 행사 전체 목록을 불러와 다가오는 활동, 초안, 지난 활동 순으로 정렬한다. 상태·유형·검색을 함께 적용하고 뒤로 가기 시 복원한다.
- 행사 카드에 담당, 학기, 신청 마감, 대기 인원과 참가자 관리 동선을 표시한다.
- 행사 작성 화면을 기본 정보 / 일정 / 정원·참가비 / 모집 상태로 나눈다. 선택 질문은 접어둔다.
- 신청 화면의 ‘신청서로 이동’은 개인 접근 값이 들어 있는 URL을 변경하지 않고 입력란으로 이동한다.
- 실제 입출금 장부를 기본으로 펼쳐 저장한 기록과 삭제 버튼을 바로 확인하게 한다.
- 행사, 참가 신청, 재고 품목, 회의록, 결정·할 일, 게시글, 입출금 기록에 X 버튼을 추가한다. 부원·지출 계획·역할·임원은 기존 X 기능을 유지한다.

### 삭제의 의미와 연결 자료

새 삭제 기능은 목록에서 제외하고 원본·삭제자·삭제 시각을 보관하는 방식이다. 공개 게시글은 비공개 처리하고 삭제된 행사·신청의 접근 링크를 차단한다. 개인정보 영구 정리는 기존 학기말 정보 정리 절차를 사용한다.

- 행사: 초안·취소·완료 상태에서만 삭제한다. 진행 중 신청, 대기·승급 예약, 미납·미환불이 남으면 먼저 정리하도록 안내한다.
- 참가 신청: 취소·만료 후 미환불이 없을 때 삭제한다. 기존 재신청 시 이력 보관을 유지한다.
- 장부: 잔액 계산에서 제외한다. 납부·환불 합계, 회비 거래 연결, 지출 계획 집행 상태를 같은 트랜잭션에서 정정한다. 이미 환불한 납부 기록은 환불 기록을 먼저 정정해야 한다. 실제 은행 거래는 실행하지 않는다.
- 재고: 수량을 먼저 정리한 품목만 삭제하며 입출고 이력은 보관한다.
- 회의: 살아 있는 결정·할 일 연결을 먼저 해제해야 한다.
- 원본을 보관하므로 삭제한 동일 거래 요청을 재전송해도 중복 반영되지 않는다.
- 감사 로그, 입출고 이력, 수정 이력과 필수 운영 설정에는 일반 삭제 버튼을 제공하지 않는다. 추적 근거를 유지하기 위한 구분이다.

## [4] 변경 파일

`web/src/admin.js`, `admin-forms.js`, `main.js`, `public.js`, `style.css`; `functions/src/deletion.js`, `service.js`; `tests/integration.test.mjs`, `tests/browser/discovery-deletion.spec.js`, `service.spec.js`; 이 문서와 생성된 Pages 산출물.

## [5] 위험 등급

- High: 사용자 실행 시 실제 운영 기록과 장부 합계에 영향을 주는 기능이다. 확인 화면, 업무 권한, 연결 자료 검사, 트랜잭션, 삭제 이력으로 보호한다.
- Medium: 원본 보관 방식이므로 개인정보 영구 삭제와 구분해야 한다. 문서와 확인창에 명시했다.
- Low: 메뉴와 화면 배치 변경에 대한 익숙함의 차이. 모바일·데스크톱 검증을 수행했다.

## [6] 검증

- 도메인 테스트 9개 통과.
- 통합 테스트 46개 통과: 권한·동시성·중복 방지·정원·정산·삭제·자료 연결·개인정보 정리 포함.
- 기존 서비스·UX 브라우저 검증 28개 중 장부 접힘에 따른 2개 실패를 수정하고 해당 2개 재실행 통과. 나머지 26개 통과.
- 새 메뉴 탐색·필터 복원·삭제 확인·장부 잔액 검증 6개 통과(PC·모바일).
- 신청서 바로가기 이후 접근 URL과 포커스를 유지하는 검증을 추가하고 PC·모바일 2개를 재실행해 통과했다. 브라우저 검증은 중복 재실행을 제외하면 총 34개 시나리오다.
- 빌드·구문·diff 공백·비밀 정보 검사 통과. PC·모바일 행사 화면 스크린샷 확인.
- Callable HTTP smoke 통과. 공개 사이트와 Firebase Hosting의 운영 화면 HTML은 HTTP 200이며 JS·CSS의 SHA-256이 로컬 빌드와 일치한다.

## [7] 수행하지 않은 작업

실제 운영 데이터 삭제·정정·일괄 수정, 임원 권한 변경, IAM·키·과금·도메인 변경을 실행하지 않았다. 테스트 데이터 변경은 demo 프로젝트의 로컬 에뮬레이터에서만 수행했다.

## [8] 승인 및 수동 작업

2026-09-09 프로젝트 지침의 기존 main 반영·GitHub Pages·Firebase 배포 승인을 적용한다. 추가 운영 데이터 삭제 승인을 가정하지 않는다. 실제 기록 삭제는 운영진이 X 버튼에서 대상과 영향을 확인하고 실행한다. 기능 사용을 위해 추가 수동 작업은 필요하지 않다. Artifact Registry 이미지 정리 정책은 기존 미설정 상태를 유지했으며, 필요하면 별도 인프라 변경 승인 후 설정할 수 있다.

## [9] 병합·배포 체크리스트

- [x] 기능 브랜치에서 변경하고 기존 작업 보존
- [x] 비밀 검사와 서버·브라우저 검증
- [x] 연결 자료 및 회계 합계 검사
- [x] Firebase Functions를 클라이언트보다 먼저 배포
- [x] Firebase Hosting과 GitHub Pages 산출물 배포
- [x] main 반영 및 배포 상태 확인

## 배포 기록

- Firebase Functions `martini-v2:martiniApi`, `asia-northeast3`: Successful update operation 확인. CLI는 함수 업데이트 성공 후 Artifact Registry 이미지 정리 정책 미설정으로 종료 코드 1을 반환했다. `--force`나 정리 정책 설정 명령은 실행하지 않았다. 함수 업데이트 실패와 구분한다.
- Firebase Hosting `martini-class-d4d69`: release complete / Deploy complete 확인.
- GitHub Pages 실행 `34347643431`: 구현 커밋 `34726468dc9705fc657a1b2872643cfa02bcefff`, success 확인.
- `https://hyu-martini.site/admin/events/`와 `https://martini-class-d4d69.web.app/admin/events/`: HTTP 200, 새 JS `index-DIElWUuH.js`와 CSS `index-x3nRl0J1.css`의 파일 해시 일치 확인.
- 실제 로그인 사용자 세션으로 운영 자료를 삭제하는 테스트는 하지 않았다. 삭제 동작은 로컬 에뮬레이터에서 검증했다.
