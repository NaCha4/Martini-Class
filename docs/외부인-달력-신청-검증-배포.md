# 외부인 달력 신청 검증·배포

2026-09-12. 외부인 출입 신청을 월간 달력으로 선택하는 화면으로 개선한다.

## [1] 브랜치와 Git 상태

`codex/visit-calendar`에서 작업. 시작 커밋 `cf47740`은 확인 당시 원격 main과 일치했다. 기능 커밋 `6a4022b`를 main에 반영했고 작업 트리는 깨끗했다. 후속 보고서 커밋은 배포 산출물을 변경하지 않는다.

## [2] 키·비밀 정보·권한 검사

비밀 정보 검사 통과. 추적 환경 파일은 안전한 예시 `.env.example`뿐이다. `.env.local`, `serviceAccount.json`, `.secrets`가 없고 해당 경로는 ignore 처리되어 있다. 추적 소스에서 실제 개인 키·서비스 계정 서명은 발견하지 않았다. 기존 공개 Firebase 설정과 서버 권한·보안 규칙을 유지한다.

## [3] 변경 내용

- 월간 달력에서 날짜 선택, 오늘·선택 날짜 표시, 이전·다음 달 이동.
- 지난 날짜 및 90일 이후 날짜 선택 제한, 월 이동 시 입력 보존.
- 시작·종료 시간, 인원, 방문 사유, 외부인 이름 입력과 개인정보 동의.
- 다음 날 종료 선택 및 선택 날짜·시간·인원 요약. 기존 한국 시간·최대 12시간 검증 유지.
- 데스크톱은 달력과 입력란을 나란히, 모바일은 달력을 먼저 표시.
- 기존 부원 확인, 운영진 승인, 신청 취소, 개인 확인 링크 호환.

## [4] 변경 파일

`web/src/member-portal.js`, `web/src/visit-calendar.js`, `web/src/member-portal.css`, `tests/browser/member-portal.spec.js`, `docs/부원-라운지.md`, 이 보고서 및 GitHub Pages 정적 산출물.

## [5] High / Medium / Low

- High: 신규 비밀 노출·접근 확대 발견 없음. 운영 배포는 기존 프로젝트 승인 범위.
- Medium: 이번 변경에서 신규 인증·서버 호환 위험 발견 없음. 기존 명부 대조 방식은 유지.
- Low: 클라이언트 UI 변경. 새 화면도 기존 서버의 방문 시간·인원·권한 검증을 적용받는다.

## [6] 검증

- 단위 테스트 11/11, 부원 라운지 서버 통합 테스트 18/18 통과.
- PC·모바일 브라우저 10/10 통과. 신청·승인·취소, 문의·개인 확인 링크, 행사 신청 회귀 검증 포함.
- 최종 달력 테스트 PC·모바일 2/2 재검증. 미선택 오류, 월 이동 입력 보존, 90일 경계, 잘못된 종료 시간·12시간 초과 거부, 익일 방문 요청의 KST→UTC 값 확인.
- 연말·윤년 익일 날짜와 한국 자정 경계 확인. PC·모바일 화면 및 가로 넘침 없음 확인.
- 빌드, 비밀 검사, JavaScript 구문 및 diff 공백 검사 통과.

## [7] 수행하지 않은 작업

운영 테스트 신청·데이터 수정·삭제, IAM·키·과금·도메인·보안 규칙·App Check 변경 없음. 가상 데이터 작성은 `demo-*` 로컬 에뮬레이터에서만 수행했다. 서버 함수는 변경하지 않았다.

## [8] 승인·수동 작업

`AGENTS.md`의 2026-09-09 지시에 따라 main 반영 및 Firebase·GitHub Pages 배포는 사전 승인된 범위이다. 추가 운영 조치는 필요하지 않다.

## [9] 병합·배포 점검

- [x] 원격 main 기준 확인, 기능 브랜치 작업
- [x] 비밀 검사, 관련 테스트, 모바일·PC 화면 확인
- [x] 정적 배포 산출물 생성
- [x] main 반영 및 GitHub Pages 빌드 성공
- [x] Firebase Hosting 배포
- [x] 양쪽 실제 사이트의 HTML·JS·CSS 버전과 공개 화면 확인

Firebase Hosting 배포 성공, GitHub Pages 실행 `34682758481` 성공. 2026-09-12 08:15 UTC에 `hyu-martini.site`와 `martini-class-d4d69.web.app`의 `/members/` HTTP 200 및 JS/CSS 바이트 일치를 확인했다. 두 사이트 모두 PC·모바일에서 ‘달력에서 날짜 선택’ 표시, 부원 확인 진입, 화면 오류·가로 넘침 없음을 검증했다. 운영 명부를 이용한 인증·신청 제출은 수행하지 않았으며 전체 신청 흐름 검증은 로컬 에뮬레이터 결과이다.
