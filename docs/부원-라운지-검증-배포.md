# 부원 라운지 검증·보안·배포 보고서

2026-09-12. 상세 동작은 [부원 라운지 운영 안내](부원-라운지.md)를 따른다.

## [1] 현재 브랜치와 Git 상태

`codex/member-lounge-guest-approvals`에서 구현했다. 최신 `origin/main`의 `bc7ced7`까지 통합하여 관리인원 공통 금액, 결정 카테고리, 명부 내보내기 변경을 보존했다. 작업 시작 당시의 행사 안내 문구 수정은 최신 main에도 반영돼 있어 그대로 유지했다. 기능 소스 커밋은 `eace491`이다.

## [2] 키·비밀 정보·권한 검사

비밀 키 서명 없음. `.env`, `.env.local`, `.secrets`, 서비스 계정 키 파일 없음. 해당 파일 이름과 private key 확장자는 Git ignore 적용. `.env.example`은 안전한 예제이며 Firebase Web 설정 및 App Check 사이트 키는 공개 클라이언트 설정이다. 실제 비밀 값은 로그·문서·커밋에 포함하지 않았다.

유효한 임원 임기와 기존 명부 관리 권한을 서버에서 검사한다. 기존 Firestore/Storage 직접 접근 차단과 App Check 설정을 유지했다. IAM·임원 배정·키를 변경하지 않았다.

## [3] 변경 내용

부원 라운지, 현재 행사 조회·신청, 공지, 외부인 출입·가입·문의 접수, 내 신청 내역과 개인 확인 링크를 제공한다. 간부는 신청 · 문의에서 승인·반려·답변한다. 부원 세션은 2시간이며 현재 학기·명부 정보가 변경되면 무효화된다. 같은 명부 행을 다른 사람에게 재할당해도 이전 사람의 신청을 조회하거나 승인할 수 없다.

중복 접수와 응답 유실은 요청 ID·확인 값으로 복구한다. 승인·취소는 트랜잭션과 수정 버전으로 충돌을 차단한다. 공개 개인 링크의 확인 값은 URL fragment에만 들어간다.

## [4] 변경 파일

- 서버: `functions/src/member-portal.js`, `functions/src/service.js`, `firestore.indexes.json`
- 화면: `web/src/member-portal.js`, `member-portal.css`, `admin-requests.js`, `admin-requests.css`, `admin.js`, `public.js`, `main.js`, `ui.js`
- 빌드·테스트: `scripts/build.mjs`, `package.json`, `tests/member-portal.test.mjs`, `tests/browser/member-portal.spec.js`
- 문서: `README.md`, `SECURITY.md`, 이 보고서와 운영 안내
- 공개 산출물: `.pages-assets.json`, 루트·경로별 HTML, 버전별 JS/CSS. 새 `/members/`, `/admin/requests/` 포함

## [5] High / Medium / Low

- High: 새 비밀 정보 노출·공개 개인정보 조회·무권한 승인 결함은 검증에서 발견되지 않았다. 운영 배포는 프로젝트의 기존 명시적 승인 범위로 수행한다.
- Medium: 부원 확인은 명부 정보 대조이며 휴대전화 소유 인증이 아니다. 새 신청·세션의 보존기한 정리는 수동 운영 대상이다. 기존 callable App Check 강제 적용 여부를 이번 기능에서 변경하지 않았다.
- Low: 공개 Firebase 설정은 유지한다. 로컬 Node 24에서 검증하고 운영 함수는 기존 Node 22를 사용한다.

## [6] 검증 결과

- 단위 테스트 11/11 통과.
- 최신 main 통합 후 전체 에뮬레이터 통합 테스트 92/92 통과. 새 부원 라운지 테스트 16개 포함.
- callable HTTP 검사 통과: 운영자 로그인·조회, 공개 조회, 미인증 차단.
- 비밀 정보 검사 및 `git diff --check` 통과.
- `build:pages` 성공, 공개 파일 35개 생성.
- 신규 브라우저 8/8 및 기존 회귀 4/4 통과. 데스크톱·모바일에서 출입 승인·취소, 가입 반려, 문의 답변, 응답 유실 후 복구, 개인 확인 링크 복원, 행사 신청·취소를 검증했다.
- 최종 화면 캡처에서 가로 넘침, 미등록 아이콘, 자바스크립트 오류 없음. 작은 화면의 문장 간격도 확인했다.

## [7] 수행하지 않은 작업

운영 데이터의 테스트 접수·승인·수정·삭제·이관, IAM·키·과금·도메인·App Check 강제 적용 변경, TTL 설정, 자동 알림 발송, force push를 실행하지 않았다. 실제 행사·공지·명부에 가상 데이터를 넣지 않았다.

## [8] 승인·수동 운영 사항

`AGENTS.md`의 2026-09-09 지시에 따라 검증 후 main 반영과 GitHub Pages·Firebase 배포는 사전 승인된 작업이다. 추가 배포 승인을 요청하지 않는다. 운영진은 현재 명부와 공지를 관리하고, 가입 승인 후 실제 명부 등록 및 보존기한 지난 새 신청 기록 정리를 별도로 수행한다.

## [9] 병합·배포 점검

- [x] 최신 main 변경 보존 및 충돌 해결
- [x] 비밀 정보·권한·입력·동시성·기존 기능 통합 검증
- [x] GitHub Pages 정적 경로와 산출물 생성
- [x] 신청별 조회 복합 색인 배포
- [x] 색인 READY 확인 및 Firebase 함수 배포
- [x] 데스크톱·모바일 브라우저 최종 검증
- [ ] main 반영과 GitHub Pages 배포 확인
- [ ] 실제 공개 페이지·미인증 차단 확인

Firebase `martini-v2:martiniApi`는 2026-09-12 07:22:35 UTC 업데이트 후 ACTIVE 상태이며 Node 22를 유지한다. 배포 CLI는 함수 업데이트 성공 후 기존 Artifact Registry 이미지 정리 정책 미설정으로 종료 코드 1을 반환했다. 실제 함수 ACTIVE와 색인 READY를 API로 재확인했다. 정책 생성·`--force`·이미지 삭제는 실행하지 않았다. 첫 필터 `functions:martiniApi`는 코드베이스가 일치하지 않아 변경 없이 중단됐고, `functions:martini-v2:martiniApi`로 수정해 배포했다.
