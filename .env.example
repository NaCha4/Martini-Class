# Public Firebase Web configuration is in web/src/firebase.js.
# Production uses the existing App Check registration for hyu-martini.site.
# Local development always uses demo-martini emulators.
# Never add service-account or admin credentials to VITE_* variables.
VITE_APPCHECK_ENABLED=true
