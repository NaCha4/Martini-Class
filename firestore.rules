rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All v2 reads and writes go through validated callable functions.
    // Existing data is preserved; deploying these rules disables the legacy client.
    match /{document=**} { allow read, write: if false; }
  }
}
