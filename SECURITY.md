# 보안 및 운영 기준

## 2026-09-11 결정 · 할 일 전용 카테고리

- 결정·할 일은 활동 운영 행사와 분리된 martini_v2_decisionCategories와 categoryId로 분류한다. 새 화면은 행사 목록을 조회하거나 행사를 생성하지 않는다. 기존 eventId 기록과 분류가 없는 기록은 일괄 수정 없이 공통 업무에서 표시한다.
- 카테고리 조회·생성·삭제는 유효한 임원 계정과 기존 결정 권한을 검사한다. 활동 운영 행사, 참가자, 재무 권한은 부여하지 않는다. Firestore/Storage 클라이언트 직접 접근 차단을 유지한다.
- 카테고리 생성은 이름 길이와 공백을 검증하고, 요청 ID와 이름 예약 문서를 트랜잭션으로 처리해 재시도·동시 중복 생성을 방지한다. 이름과 ID, 버전만 목록으로 반환한다.
- 삭제는 카테고리에 삭제 표시와 감사 기록을 남긴다. 결정·할 일과 수정 이력은 변경하거나 삭제하지 않는다. 삭제된 분류의 기록은 공통 업무에 표시하고, 같은 이름을 다시 만들면 새 ID를 사용해 이전 업무가 다시 연결되지 않도록 한다.
- 결정 저장은 카테고리 존재 여부와 삭제 상태를 트랜잭션으로 검증한다. 삭제와 저장의 경합을 보호한다. 구형 클라이언트가 categoryId를 생략해도 기존 카테고리를 보존한다. 삭제된 분류의 기존 기록을 수정하면 공통 업무로 정리한다.
- 서버 선배포와 이미 열린 클라이언트의 호환을 위해 기존 decisionEvents API와 eventId 입력을 당분간 유지한다. 새 화면에서는 사용하지 않는다. 카테고리는 업무 분류이며 별도 접근 권한 경계는 아니다.
- 운영 자료 일괄 이전, 보안 규칙·IAM·키 변경은 필요하지 않다. 요청 기능과 배포는 AGENTS.md의 기존 승인 범위에서 진행한다.

## 공개 설정과 비밀 정보

web/src/firebase.js의 Firebase Web 설정과 App Check 사이트 키는 공개 클라이언트 설정이다. 서비스 계정 키, OAuth 토큰, 실제 .env 값, 비밀번호는 저장소·로그·회의록·채팅에 남기지 않는다. .env.example은 안전한 옵션만 제공한다.

최초 운영자 준비 스크립트는 Firebase CLI의 기존 OAuth 세션을 메모리에서 사용한다. 요청·응답 본문 로그를 끄고 결과 상태만 출력한다. 임시 비밀번호는 출력하거나 파일에 저장하지 않는다.

## 데이터 접근

- Firebase Auth 로그인 후 martini_v2_admins/{uid}의 역할·활성 상태·임기를 확인한다.
- 모든 앱 데이터 요청은 martiniApi callable을 통한다.
- Firestore와 Storage의 클라이언트 직접 접근은 모두 차단한다.
- Admin SDK는 서버에서만 실행하며 입력 필드·자료형·길이·역할·신청 기간·활동 자격을 확인한다.
- 부원 목록·학번·전화번호는 공개 API로 제공하지 않는다.
- 정원·신청 상태·재고·장부는 트랜잭션으로 일관성을 유지한다.
- 수정 버전과 요청 ID로 오래된 덮어쓰기·중복 차감·중복 장부를 방지한다.

## 역할

| 역할 | 범위 |
| --- | --- |
| owner (회장) | 전체 업무, 임원 권한, 개인정보 정리 |
| chair (부회장) | 회장단 업무, 설정, 이력 |
| education | 행사·재고·회의·결정 |
| execution | 행사·명부·재고·회의·결정 |
| finance (재무부) | 명부·회비·정산·참가자 확인·회의·결정 |
| publicity | 공개 콘텐츠·회의·결정 |

로그인 공급자가 비밀번호라는 이유만으로 임원 권한을 부여하지 않는다. UI 메뉴 숨김과 별개로 서버가 매 요청 권한을 확인한다.

## App Check와 남용 방지

기존 프로젝트는 Auth·Firestore·Storage의 App Check를 이미 강제하고 있다. 이 설정을 유지했다. 프로덕션 웹은 등록된 reCAPTCHA Enterprise 공급자를 초기화하며 실제 허용 도메인은 hyu-martini.site이다.

새 martiniApi 함수의 enforceAppCheck는 false이다. 함수의 강제 적용은 실제 사용자 지표 확인과 별도 승인 후 진행한다. 임원 업무는 Auth+UID 역할 검증, 공개 신청은 링크·명부·등록 학기·본인 정보와 요청 제한으로 보호한다.

- 공용 Wi-Fi를 고려해 IP 제한을 분산하고 부원 식별값별 요청 제한을 둔다.
- 운영 함수는 CPU 1, 512MiB, 최대 1개 인스턴스, 동시 요청 100, 제한 시간 60초이다.
- 행사별 메모리 대기열은 과도한 경합을 줄인다. 배포 교체 중의 여러 인스턴스에서도 Firestore 트랜잭션이 정원을 보장한다.
- rateLimits에는 해시 식별자·카운트·만료 Timestamp를 저장한다. expiresAt의 TTL 설정은 별도이며, 필드만으로 자동 삭제되지 않는다.
- API 키는 기존 키를 유지했고 Auth 및 Secure Token API 허용을 확인했다.

## 개인정보 보존

학기말 정리는 owner만 사용할 수 있다. 대상 조회 → 건수·미정산 검토 → 확인 문구·사유 입력 → 적용 순서이다. 미리보기 후 자료가 바뀌면 중단한다.

- 선택 학기의 신청·재신청 이력 이름과 답변, 연결 장부의 제목·메모를 지운다.
- 명부 자체를 정리할 때는 모든 학기의 미정산·진행 중 신청을 확인한다.
- 새 학기 명부는 유지하면서 이전 학기 기록만 정리할 수 있다.
- 금액·행사 집계·내부 연결 ID는 보존하므로 법률상 완전한 익명화를 보장하는 기능은 아니다.
- 정리된 신청으로 현재 명부의 연락처를 다시 조회하거나 개인 확인 링크를 재발급할 수 없다.
- 회의·결정 본문, 공개 글, 변경 사유의 자유 입력, 외부 CSV·백업은 담당자가 별도로 검토해야 한다.
- 한 대상의 연결 문서가 350개를 넘으면 자동 처리를 차단한다.

실제 운영 자료에 이 정리 기능을 실행하지 않았다.

## 승인 범위와 운영 변경

사용자가 제공한 Global Security And Operations Policy를 따르되, 2026-09-09의 최신 지시에 따라 요청한 코드 변경의 검증·main 반영·GitHub Pages 및 Firebase 배포는 사전 승인된 것으로 처리한다. 매 배포마다 별도 승인을 요청하지 않는다. 이 승인은 관련 없는 운영 데이터 정리나 키·IAM 변경을 자동 실행하라는 지시가 아니다.

이번 작업에서 승인된 범위:
- 완성한 서비스의 main push와 테스트
- Firebase에 필요한 API 활성화와 함수·Firestore/Storage 규칙 배포
- 현재 CLI 로그인 이메일로 최초 앱 owner 생성과 비밀번호 설정 메일 요청

별도 명시 승인이 필요한 실행:
- 기존 운영 자료의 이관·수정·삭제·일괄 정리
- IAM, 프로젝트 소유·관리 권한, 키 생성·교체·삭제
- 과금·할당량·도메인·기타 운영 인프라 변경
- App Check 강제 적용 변경
- force push

최초 계정 스크립트는 기본 dry run이며 --apply가 있어야 실행한다. owner가 이미 존재하면 덮어쓰지 않는다. 메일 발송 도구도 --send를 명시해야 한다.

## 전환과 복구

기존 데이터는 martini_v2_* 밖에 보존한다. 새 차단 규칙을 적용한 후에는 이전 클라이언트가 직접 Firestore/Storage를 사용하는 방식으로 돌아갈 수 없다. 화면만 과거 버전으로 되돌려도 데이터 접근이 복구되는 것은 아니다.

GitHub Pages의 기존 main/root 설정과 CNAME은 유지한다. npm run build:pages가 공개 HTML과 assets를 만든다. 서버와 규칙의 변경은 Firebase 배포 이력에서 별도로 검토한다.

## 병합·배포 체크리스트

- [x] 최신 사용자 결정·제공 자산 반영
- [x] 개발/운영 데이터 분리, 비밀 정보 기본 검사
- [x] 권한·정원·대기·회비·재고·회의·개인정보 검증
- [x] 데스크톱·모바일 Chrome 주요 흐름 검증
- [x] 함수·규칙·계정 준비 실행 승인
- [x] 기존 App Check 보호와 도메인 유지
- [x] 실제 운영자가 일반 Chrome에서 로그인 성공 확인
- [ ] 실제 가입 오픈채팅·문의 채널·명부·재고 입력
- [ ] 후임 관리자, 백업·보존, rateLimits TTL, 빌드 이미지 정리 정책 확인

## 2026-09-09 가입 안내와 운영 설정 변경

- 가입 화면은 카카오톡 오픈채팅으로 연결하고 가입 방법은 운영진이 채팅에서 안내한다. 홈페이지에서 회비·가입 기간·승인 절차를 정하지 않는다.
- 설정에는 현재 학기(기록 구분), 활동 장소, 가입 오픈채팅 링크, 문의 채널, 소개만 입력한다. 링크가 없거나 카카오 오픈채팅 주소가 아니면 준비 중으로 표시한다.
- 회비·기간·기본 납부 안내는 설정에서 입력하지 않는다. 기존 값은 일반 설정 수정 시 보존하지만 새 화면이나 회비 금액 검증에 사용하지 않는다. 실제 회비 입금액은 권한 있는 운영진이 장부에서 확인·기록한다.
- 개인정보 처리방침은 GitHub 96b847cff685d1f1f8892dd8208775ead2eaf87c의 legal/privacy.html 본문을 web/src/content/privacy.html에 그대로 보존한다. 원본: https://github.com/NaCha4/Martini-Class/blob/96b847cff685d1f1f8892dd8208775ead2eaf87c/legal/privacy.html
- 정책의 담당자 연락처 ‘추후 기재’ 등 원문을 임의로 바꾸지 않았다. 개인정보 페이지는 데이터베이스 연결 없이도 표시한다.
- 홈페이지 정책 문구 복원은 개인정보 자동 정리 작업을 만들거나 기존 운영 기록을 수정하지 않는다. 담당자가 정책에 따라 보존 여부를 검토하며 현재 학기는 정리할 수 없다. 기존 owner 전용 검토·확인·미정산 차단은 유지한다.
- 회비 납부 확인에 따른 행사 활동 자격, 행사별 참가비·신청 기간·정원, 임원 권한은 별도 기능으로 유지한다.

2026-09-09: 사용자가 이번 martiniApi 함수 변경 배포를 명시적으로 승인했다. 단위 테스트 9개, 에뮬레이터 통합 테스트 26개, PC·모바일 브라우저 30개 시나리오를 검증했다. 변경된 회비 입력 테스트와 준비 중 표시 선택자를 수정한 4개 시나리오는 재실행하여 통과했다.

## 2026-09-09 역할 관리와 부원 등록 변경

- 부원 등록 자체가 가입·회비 확인 완료를 뜻한다. 화면·CSV에서 회비 납부 체크와 활동 상태를 제거하고 서버의 신청·대기 승급·수락에서 두 값을 자격 조건으로 사용하지 않는다. 등록 학기와 본인 정보, 개인정보 정리 여부는 계속 확인한다.
- 기존 status/duesPaid 필드는 이전 클라이언트 호환을 위해 입력은 허용하되 새 등록·수정 시 저장하지 않으며 조회에도 노출하지 않는다. 운영 DB 일괄 수정·삭제나 이전 회비 장부 이관은 실행하지 않는다.
- 성별 입력은 남성·여성 또는 미선택이며 기타 선택지는 없다.
- 역할 관리(/admin/roles)에서 역할을 생성·수정하고 임원 배정(/admin/admins)에서 배정한다. 사용자 정의 역할은 권한 관리 계정만 만들 수 있다.
- 기본 역할 표시는 owner=회장, chair=부회장, finance=재무부이며 기존 계정의 역할 코드는 변경하지 않는다. 기본 회비·정산 권한은 이 세 역할에만 있다. 사용자 정의 역할에 재무 권한을 주려면 회장이 명시적으로 선택해야 한다.
- 역할별 명부·행사·회계·재고·회의·결정·게시·설정·이력 권한은 서버에서 매 요청 다시 확인한다. 화면을 이동하거나 새로 읽을 때 권한과 메뉴를 갱신하고 권한 변경 시 화면 캐시를 비운다.
- 회비·정산 권한이 없으면 장부 조회·처리가 거부되고, 신청자 목록·상세의 납부 상태·납부액·환불액도 서버 응답에서 제외한다. 행사 안내용 참가비와 취소 정책은 행사 담당자가 확인할 수 있다.
- 역할·임원 관리 및 개인정보 정리는 회장의 필수 권한으로 유지한다. 회장 역할 자체의 변경, 자기 자신의 최종 관리 권한 해제, 배정 중인 역할 삭제, 중복 이름 및 오래된 버전 덮어쓰기를 차단한다.
- 역할과 임원 배정 기능은 앱 내부 권한만 관리한다. Firebase IAM·서비스 계정·Auth 계정 생성 기능과 분리되어 있다.


## 2026-09-09 학기별 부원 명부

- Firestore 저장 경로는 `martini_v2_semesters/{학기}/members/{부원ID}`이다. 예: `martini_v2_semesters/2026-2/members/{부원ID}`. 학생 문서에는 semester 필드를 저장하지 않는다. API가 구형 화면과의 호환을 위해 경로에서 학기를 계산해 응답할 수 있다.
- 명부 화면에서 학기 폴더를 선택한다. 다른 학기 열기로 새 학기를 열고 첫 부원을 등록하면 학기 폴더가 보존된다. 개별 등록·수정 화면과 표에는 등록 학기 칸을 두지 않는다. CSV도 선택 학기만 포함하며 파일명에 학기를 표시한다.
- 같은 학생의 다른 학기 가입은 별도 기록이다. 과거 명부를 다음 학기로 덮어쓰지 않는다. 행사 신청·대기 승급·연락처 조회는 행사 학기의 명부만 참조한다. 학기 회비 거래 연결은 `martini_v2_semesters/{학기}/dues/{부원ID}`에 보존하며 회비 금액 자체는 기존 장부에 남긴다.
- 개인정보 정리는 선택 학기의 명부와 연결 기록에만 적용한다. 다른 학기의 학생 정보는 유지한다. 기존 역할·서버 권한 검사와 클라이언트 직접 접근 차단은 유지한다.
- `node scripts/migrate-semester-roster.cjs`는 운영 명부 이전을 미리 점검하고 건수만 출력한다. `--apply`는 학생 ID·내용과 회비 장부 연결을 보존해 트랜잭션으로 옮기고 이전 경로를 제거한다. 충돌 시 덮어쓰지 않고 중단한다. 재실행해도 이미 옮긴 명부를 중복 생성하지 않는다. 기존 Firebase CLI 인증을 메모리에서 사용하며 개인정보·토큰·키는 출력하거나 파일에 저장하지 않는다.
- 배포 순서: 단위·통합·브라우저 검증 → 호환 읽기가 있는 서버 배포 → 이전 dry run → 이번 사용자 요청에 따른 명부 이전 → main 및 Pages 반영 → 실제 서비스 확인. 학기별 저장을 요청한 범위 외 운영 데이터는 변경하지 않는다.


## 2026-09-09 명부 제거·복구와 부원 메모

- 명부 관리 권한으로 선택 학기의 부원을 명부에서 제거하거나 복구한다. 화면에서 이름·학기를 확인하고 실행하며 서버는 권한·학기·수정 버전을 검증한다. 제거는 removedAt/removedBy 표시로 기록하고 일반 명부·인원 집계·신규 신청·대기 승급에서 제외한다. 기존 신청·정산·연락처 연결과 다른 학기의 명부는 유지한다. 개인정보 영구 삭제 기능과는 다르며 학기말 정리 대상에 계속 포함된다.
- 제거한 부원 보기에서 복구하면 같은 학생 ID가 유지된다. 같은 학번·연락처로 새 기록을 중복 생성하지 않는다. 개인정보를 이미 정리한 부원은 복구할 수 없다. 제거·복구는 변경 이력에 학기와 처리자를 남긴다.
- 부원 메모는 해당 학기 학생 문서의 note(최대 3000자)에 저장한다. 이름을 클릭한 상세 화면과 수정 폼에서만 표시한다. 일반/제거 목록 API 응답·목록 검색·CSV·공개 신청/영수증·행사 담당자의 연락처 조회에는 포함하지 않는다. 명부 조회 권한이 있어야 상세 메모를 읽을 수 있다. 개인정보 정리 시 메모도 비운다.
- 이번 작업은 기능 배포만 수행한다. 운영 중인 특정 부원을 실제로 제거하지 않으며, 이전에 승인 대기 중인 기존 명부 경로 이전도 실행하지 않는다.

## 짧은 신청·개인 확인 링크

- `/e/#…`와 `/r/#…`는 사용자 요청(2026-09-11)에 따라 새 링크는 96비트 무작위 키를 base64url 16자로 표시한다. 기존 256비트 키도 호환한다. 기존 키의 접두사만 사용하거나 순번으로 대체하지 않는다. 전체 주소는 44자이며 무작위 키 길이 축소는 사용자 승인 범위이다.
- 키는 URL fragment에 두어 페이지 요청 경로나 Referer에 포함되지 않게 한다. 링크 전체는 여전히 접근 권한이므로 로그·분석 이벤트에 기록하지 않는다.
- `resolveLink`는 요청 제한과 엄격한 입력 검증 후 기존 해시 필드의 정확한 일치로 ID만 반환한다. 실제 조회·신청·취소는 기존 API에서 다시 검증한다.
- 삭제·익명화 기록과 중복 해시 결과는 조회를 거절한다. 재발급으로 기존 해시가 바뀌면 긴 링크와 짧은 링크 모두 만료된다.
- 기존 긴 링크는 계속 지원한다. 운영 자료 이관이나 별도 단축 URL 업체, 새로운 비밀 키 저장소는 필요하지 않다.

## 관리인원 참가비

관리자 신청 목록에서 정산 권한이 있는 임원만 관리인원 여부와 별도 참가비(0원 면제 포함)를 지정한다. 일반 참가비 스냅샷은 유지하고, 별도 금액은 입금 확인 상한·납부 완료·대기 승급·장부 정정·미정산 점검 기준에 사용한다. 관리인원 여부·별도 금액·금액 수정 버전은 개인 링크의 공개 응답 및 정산 권한 없는 조회에서 제외한다. 사용자 신청 페이지는 변경하지 않는다.

금액 수정은 트랜잭션과 별도 버전으로 동시 수정을 검사하며 변경 이력을 남긴다. 이미 확인한 입금액보다 낮게 설정하거나 취소·익명화된 신청을 수정할 수 없다. 기존 참가자를 임의로 관리인원으로 바꾸거나 운영 장부를 일괄 변경하지 않는다.
